

/**
 * Runs a SQL query on the DB. 
 * @param string stmt 
 * @param ?string[] WHERE 
 * @returns results[]|false
 */
export default function query(stmt, WHERE = [])
{
    
        return new Promise.resolve([{"userId": 0, "sessionId": "92836sbudas", "requestId": "ahbdahd", "logPath":  "r:auth-e:signUp/r:auth-e:signUp"}]);
  
}

